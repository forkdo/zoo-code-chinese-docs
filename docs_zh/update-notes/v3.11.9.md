---
description: 此补丁版本包括性能改进和 UI 更新。
---

# Zoo Code 3.11.9 发布说明 (2025-04-07)

此补丁版本引入了按配置文件的速率限制、多规则文件支持、提供商更新、API 增加以及各种改进和错误修复。
---

## 自定义 Zoo 指令 (`.roo/` 目录)

*   引入了在 `.roo/rules/`（适用于整个工作区）和 `.roo/rules-{modeSlug}/`（适用于特定模式）目录中放置多个指令文件的支持。Zoo Code 会递归读取这些目录中的所有文件，按文件名的**字母顺序**将它们追加到系统提示中。
*   这比单个文件更好地组织和管理复杂的指令集。
*   如果相应的目录存在且包含文件，则此基于目录的方法现在优先于单个的 `.roorules` 或 `.roorules-{modeSlug}` 文件。（感谢 upamune！）
*   了解更多关于 [自定义指令](/features/custom-instructions) 的信息以及这如何适用于 [自定义模式](/features/custom-modes)。

---

## 按配置文件的速率限制

*   **速率限制**设置现在为每个 [API 配置配置文件](/features/api-configuration-profiles)单独配置。以前是全局设置，现在允许您根据正在使用的提供商、模型或特定配置文件设置来设置请求之间的不同最小延迟。（感谢 ross 和 olweraltuve！）
*   默认值仍为 0（禁用），这适合大多数用户。
*   在每个配置文件的选项中配置此设置。详情请参见 [API 配置配置文件](/features/api-configuration-profiles#creating-a-profile) 指南。关于使用跟踪的一般信息可在 [速率限制和成本](/advanced-usage/rate-limits-costs) 中获得。


---

## 一般改进和生活质量改进

*   整理了 ClineProvider 重构后的内容（感谢 diarmidmackenzie！）。
*   使用高级语言结构增强了 Rust tree-sitter 解析器（感谢 KJ7LNW！）。
*   在 `api.setConfiguration` 上保留了设置（感谢 gtaylor！）。
*   为设置部分添加了深度链接。
*   添加了聚焦 Zoo Code 输入字段的命令（感谢 axkirillov！）。
*   为浏览器添加了调整大小和悬停操作（感谢 SplittyDev！）。
*   向 API 添加了 `resumeTask` 和 `isTaskInHistory`（感谢 franekp！）。
*   为 webview 开发添加了动态 Vite 端口检测（感谢 KJ7LNW！）。

---

## 错误修复

*   防止按钮出现时不必要的自动滚动（感谢 shtse8！）。
*   读取文件时限制了负行号（感谢 KJ7LNW！）。
*   修复了显示布尔值/数值建议答案的错误。

---

## 提供商更新

*   向 Vertex AI 添加了 Gemini 2.5 Pro 预览版（感谢 nbihan-mediware！）。
