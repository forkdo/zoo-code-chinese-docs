---
description: Zoo Code 2.2.31 改进了对链式命令（如 'cd some-dir && npm install'）的自动批准逻辑，以实现更好的自动化。
keywords:
  - zoo code
  - v2.2.31
  - release notes
  - command chaining
  - auto-approval
  - automation improvements
---

# Zoo Code 2.2.31 发布说明

此版本改进了对命令的自动批准逻辑。

---

## 通用和生活质量改进

*   改进了对链式命令（例如 `cd some-dir && npm install`）的自动批准逻辑。