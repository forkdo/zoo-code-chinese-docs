---
description: 学习如何有效地在大型代码库中使用 Zoo Code。管理上下文限制、优化 token 使用，并处理复杂的重构任务。
keywords:
  - large projects
  - context management
  - token optimization
  - codebase refactoring
  - Zoo Code scalability
---

# 处理大型项目

Zoo Code 可用于任何规模的项目，但大型项目需要特别注意以有效管理上下文。以下是一些处理大型代码库的技巧：

---

## 了解上下文限制

Zoo Code 使用的大型语言模型（LLM）具有有限的“上下文窗口”。这是模型一次可以处理的最大文本量（以 token 计算）。如果上下文过大，模型可能无法理解您的请求或生成准确的响应。

上下文窗口包括：

*   系统提示（Zoo Code 的指令）。
*   对话历史。
*   您使用 `@` 提及的任何文件的内容。
*   Zoo Code 使用的任何命令或工具的输出。

---

## 管理上下文的策略

1.  **具体明确：** 在引用文件或代码时，请使用具体的文件路径和函数名称。避免使用“主文件”等模糊的引用。

2.  **有效使用上下文提及：** 使用 `@/path/to/file.ts` 来包含特定文件。使用 `@problems` 来包含当前的错误和警告。使用 `@` 后跟提交哈希来引用特定的 Git 提交。

3.  **分解任务：** 将大型任务分解为更小、更易于管理的子任务。这有助于保持上下文的专注。

4.  **总结：** 如果您需要引用大量代码，请考虑在提示中总结相关部分，而不是包含整个代码。

5.  **优先考虑最近的历史：** Zoo Code 会自动截断对话历史中较旧的消息，以保持在上下文窗口内。请注意这一点，并在需要时重新包含重要的上下文。

6.  **使用提示缓存（如果可用）：** 一些 API 提供商（如 Anthropic、OpenAI、OpenRouter 和 Requesty）支持“提示缓存”。这会将您的提示缓存起来，供未来的任务使用，并有助于降低请求的成本和延迟。

---

## 示例：重构大型文件

假设您需要重构一个大型 TypeScript 文件（`src/components/MyComponent.tsx`）。以下是一种可能的方法：

1.  **初步概览：**
    ```
    @/src/components/MyComponent.tsx 列出此文件中的函数和类。
    ```

2.  **定位特定函数：**
    ```
    @/src/components/MyComponent.tsx 重构 `processData` 函数，使用 `async/await` 而不是 Promises。
    ```

3.  **迭代更改：** 进行小的、渐进的更改，审查和批准每一步。

通过分解任务并提供具体的上下文，即使上下文窗口有限，您也可以有效地处理大型文件。